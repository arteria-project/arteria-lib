

A self contained (Tornado) REST service for managing running external Sisyphus commands
such as the quick report generator and quality control suite.

Install:
    # install the qc package and create the script qc-ws
    python setup.py install

    # install the daemon, after this you can manage the service through
    # etc/init.d/qc-ws
    install-daemon


